
    int i=${num[0]};
    do{
        while(true)
        {
            while(i==${num[0]}){
                i=${num[1]};
            }
            
            if(i!=${num[1]}){
                break;
            }
            i=${num[2]};
        }
    }while(i!=${num[2]});
